apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: "person-ingress"
  namespace: test
  annotations:
   ingress.kubernetes.io/ssl-redirect: "false"


spec:
  rules:
    - host:
      http:
        paths:
          - path: /person
            backend:
              serviceName: person-service
              servicePort: 80

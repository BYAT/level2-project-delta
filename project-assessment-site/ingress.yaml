apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: "site-ingress"
  namespace: test

spec:
  rules:
    - host:
      http:
        paths:
          - path: /
            backend:
              serviceName: project-assessment-site
              servicePort: 80

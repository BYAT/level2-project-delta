apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: "office-ingress"
  namespace: delta

spec:
  rules:
    - host:
      http:
        paths:
          - path: /office
            backend:
              serviceName: office-service
              servicePort: 80

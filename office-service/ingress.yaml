apiVersion: extensions/v1
kind: Ingress
apiVersion: extensions/v1beta1
metadata:
  name: "office-ingress"
  namespace: delta

spec:
  rules:
    - host:
      http:
        paths:
          - path: /office
            backend:
              serviceName: office-service
              servicePort: 80
